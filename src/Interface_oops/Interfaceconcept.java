package Interface_oops;

public interface  Interfaceconcept {
//Interface can not have method body
	// only method declaration
	//can not create object of an interface
	//all the vars in interface are static and final
	//interfaces can have static method body
	//static methods can not be overridden
	//after JDK 1.8 method has default method
	//class extends class and implements more interfaces
	
	//class to interface implements
	//class to class extends
	//interface to interface extends
	default void Billing()
	{// default methods
		
	}
}
